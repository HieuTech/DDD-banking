server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/write?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    username: server_root
    password: ddd@banking
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update     # dev: update | production: validate/none (cẩn thận)
    show-sql: true
    properties:
      hibernate:
        format_sql: true
